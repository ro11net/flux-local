apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: databases
  namespace: flux-system
  labels:
    level: "2"
    environment: ${ENVIRONMENT}
    region: ${REGION}
spec:
  interval: 10m
  path: ./tests/testdata/deep-nested/cluster/apps/databases
  sourceRef:
    kind: GitRepository
    name: flux-system
